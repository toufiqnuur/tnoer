---
const currentYear = new Date().getFullYear()

const socialLinks = [
	{
		name: 'Gmail',
		url: 'mailto:toufiq.nuur@gmail.com'
	},
	{
		name: 'GitHub',
		url: 'https://github.com/toufiqnuur'
	},
	{
		name: 'LinkedIn',
		url: 'https://www.linkedin.com/in/toufiqnuur/'
	}
]

const siteLinks = [
	{
		name: 'Home',
		url: '/'
	},
	{
		name: 'Projects',
		url: '/projects'
	},
	{
		name: 'Uses',
		url: '/uses'
	},
	{
		name: 'About',
		url: '/about'
	}
]

const otherLinks = [
	{
		name: 'Blog',
		url: 'https://tdots.pages.dev',
		external: true
	},
	{
		name: 'Guest Book',
		url: '/guestbook',
		external: false
	},
	{
		name: 'Support Me',
		url: '/support-me',
		external: false
	}
]
---

<footer class="mt-12 bg-zinc-100 dark:bg-zinc-950/25">
	<div class="container">
		<div class="flex flex-col gap-8 py-8 md:flex-row md:items-center md:gap-32">
			<div>
				<div class="text-3xl font-bold">光</div>
				<div class="mt-4 text-xl font-semibold">Toufiq Nuur Rahman.</div>
				<div class="mt-4 max-w-48 font-medium opacity-75">
					Sleman, Special Region of Yogyakarta, Indonesia
				</div>
			</div>
			<div class="grid grid-cols-2 gap-8 md:flex-1 md:grid-cols-3 md:gap-16">
				<div>
					<div class="font-semibold">Navigation</div>
					<ul class="mt-2 flex flex-col">
						{
							siteLinks.map((site) => (
								<li>
									<a class="text-sm font-medium opacity-75" href={site.url}>
										{site.name}
									</a>
								</li>
							))
						}
					</ul>
				</div>

				<div>
					<div class="font-semibold">Others</div>
					<ul class="mt-2 flex flex-col">
						{
							otherLinks.map((item) => (
								<li>
									<a
										class="text-sm font-medium opacity-75"
										href={item.url}
										target={item.external ? '_blank' : '_self'}
									>
										{item.name}
									</a>
								</li>
							))
						}
					</ul>
				</div>

				<div>
					<div class="font-semibold">Social</div>
					<ul class="mt-2 flex flex-col">
						{
							socialLinks.map((social) => (
								<li>
									<a class="text-sm font-medium opacity-75" href={social.url}>
										{social.name}
									</a>
								</li>
							))
						}
					</ul>
				</div>
			</div>
		</div>
		<div
			class="border-t-2 border-zinc-200 py-4 text-sm font-medium opacity-75 dark:border-white/10"
		>
			&copy; 2023 - {currentYear} Toufiq. All rights reserved.
		</div>
	</div>
</footer>
